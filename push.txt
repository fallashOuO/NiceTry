@echo off
cd /d "D:\Unity\NiceTry"

:: Check if remote 'origin' is set
git remote -v | findstr "origin"
if errorlevel 1 (
    git remote add origin https://github.com/fallashOuO/NiceTry.git
)

:: Input commit message
set /p msg=Enter your commit message:

:: Git commands
git pull --rebase
git add .
git commit -m "%msg%"
git branch -M main
git push -u origin main

pause
